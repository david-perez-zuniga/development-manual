---

export async function getStaticPaths() {
  return [];
}

const { slug } = Astro.params;
const slugPath = slug || '01-stack-configuracion';

---
<BaseLayout title="Documentación">
  <main>
    <h1>Documentación</h1>
    <p>Ruta solicitada: <code>{slugPath}</code></p>
  </main>
</BaseLayout>
